<ul id='tasks'>
<h1>Tasks</h1>

  <% @tasks.each do |task| %>
    <ul>
      <li> <%= link_to task.title, task %> (<%= task.due %>)</li>
    </ul>
  <% end %>

<nav>
  <%= link_to 'All Employees', employees_path %>
  <%= link_to 'Add New Task', new_task_path %>
</nav>
